name: Scheduled Run

on:
  schedule:
    - cron: '02:00'  # Runs at 2 AM every day

jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Run script
        run: |
          chmod +x script.sh
          ./script.sh
